import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Test basique pour éviter les erreurs IDE', (WidgetTester tester) async {
    // Les tests sont désactivés pour l'instant
    expect(true, true);
  });
}
